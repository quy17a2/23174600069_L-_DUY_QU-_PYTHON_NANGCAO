from tkinter import * 

window = Tk() 
window.title("chào DHKL17A2HN") 

lbl = Label(window, text="Hello", font=("Arial Bold", 30))

window.geometry('300x100') 

lbl.grid(column=0, row=0) 
window.mainloop()